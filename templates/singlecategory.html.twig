{% extends 'base.html.twig' %}

{% block body %}

    <p>Nom:{{ category.name }} </p>
    {% for product in category.products %}
        <p><a href="{{ path('productsinfo',{id:product.id}) }}" >{{ product.name }}</a></p>

    {% endfor %}


    <a href="{{ path('products') }}">
        Liste des produits
    </a>

    {#<div class="comment">
        {{ form_start(form_comment) }}
        {{ form_widget(form_comment) }}
        {{ form_end(form_comment) }}
    </div>#}
{% endblock %}